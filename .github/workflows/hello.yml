name: Hello World Workflow

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  hello_job:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello WorldS!"

      - name: Show Date
        run: date

      - name: List Directory Contents
        run: ls -l
